{
  "name": "moe-surgeon",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "python -m pytest",
    "lint": "python -m ruff check src tests",
    "typecheck": "python -m mypy src",
    "metrics": "npm run lint && npm run typecheck && npm test"
  }
}
