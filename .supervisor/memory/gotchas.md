# Known Gotchas

- 2026-04-17: [Define canonical data contracts and JSON serialization schema] `python -m moe_surgeon` is not executable and exits `No module named moe_surgeon.__main__`, so the requested `python -m moe_surgeon`-style import/run path is currently broken (`src/moe_surgeon/__init__.py` exists but no `__main__.py`).
- 2026-04-17: [Define canonical data contracts and JSON serialization schema] `src/moe_surgeon/__main__.py:5` imports `sys` but never uses it. Running `python -m ruff check src tests` fails with `F401` (unused import). This is a real regression vs AGENTS quality gates and blocks the required lint check.
- 2026-04-17: [Define canonical data contracts and JSON serialization schema] Project-level quality command for JavaScript (`npm run lint`) is only `echo ok`, so automated verification can pass while `python -m ruff check src tests` fails. CI/verification should call the real Python lint command required by AGENTS to avoid false-green results.
- 2026-04-17: [Define canonical data contracts and JSON serialization schema] `package.json:7` wires `npm test` to `node --test src/**/*.test.js`, and the authoritative run still executes `0` tests. The configured suite does not exercise `tests/test_schemas.py`, so current machine checks would not catch the schema regressions above.
- 2026-04-17: [Define canonical data contracts and JSON serialization schema] `tests/test_schemas.py` adds negative-path cases, but it still lacks coverage for the failing `PrunePlan.model_handle` round-trip and for out-of-range/non-covering `PrunePlanItem` expert mappings, leaving key contract bugs untested.
- 2026-04-17: [Build pruning strategy layer and deterministic plan generator] PM verification criterion is unmet: the tree does not contain tracked `src/moe_surgeon/prune/strategy.py`; only `src/moe_surgeon/prune/strategies.py` exists (`ls src/moe_surgeon/prune`). Add the requested module or a compatibility wrapper and wire it through `src/moe_surgeon/prune/__init__.py`.