{
  "tasks": {
    "61e5ba78-020e-4ca8-910c-9611b9f25e47": {
      "attempts": [
        {
          "approach": "[Define strategy protocol and registry]: **Implemented**\nSubtask status:\n1. Done: added [strategies.py](/home/netsky/dev/labs/moe-surgeon/src/moe_surgeon/prune/strategies.py) with a typed `PruneStrategy` protocol, immutable strategy metadata, registry/factory lookup, and built-in `frequency`, `route",
          "success": false,
          "error": "`PlannerConstraints.__post_init__` silently coerces override keys with `int(layer)` and never validates override values, so invalid inputs like `PlannerConstraints(layer_overrides={1.5: None})` are ac",
          "verifyConfidence": 95,
          "issues": [
            "`PlannerConstraints.__post_init__` silently coerces override keys with `int(layer)` and never validates override values, so invalid inputs like `PlannerConstraints(layer_overrides={1.5: None})` are accepted as `{1: None}` instead of failing fast. That later explodes with raw `AttributeError` in `canonical_payload()`/`plan_constraints()` (`src/moe_surgeon/prune/planner.py:65-123`). Validate key types and require `LayerConstraintOverride` instances with explicit planner diagnostics.",
            "`LayerConstraintOverride.__post_init__` only checks numeric bounds, not integer types. `LayerConstraintOverride(min_experts=1.2)` is accepted and `build_prune_plan()` later crashes with raw `TypeError: slice indices must be integers...` in `_selected_keep_indices()` (`src/moe_surgeon/prune/planner.py:41-53`, `284-305`). Reject non-integer constraint values up front.",
            "Per-layer exact targets can undercut the configured minimum-survivor guardrail. `build_prune_plan(..., constraints=PlannerConstraints(min_experts_per_layer=2, layer_overrides={0: LayerConstraintOverride(target_experts=1)}))` succeeds and emits a plan keeping only one expert, because `_layer_budgets()` overwrites `minimum_keep` with `target_experts` (`src/moe_surgeon/prune/planner.py:233-240`). This violates the task’s minimum-survivor requirement and the PM criterion around per-layer targets below `min_experts_per_layer`.",
            "`tests/test_prune_planner.py` does not cover the failing constraint-validation branches above, so the suite stays green while invalid planner inputs still crash or bypass guardrails. Add regression tests for non-integer/invalid `layer_overrides`, non-integer `LayerConstraintOverride` values, and per-layer `target_experts` below `min_experts_per_layer`.",
            "PM-requested strategy coverage is still incomplete in tests: the file exercises missing inputs and one partial-coverage case, but it does not add explicit regressions for duplicate `expert_stats` inputs or combined-strategy activation payload mismatches in `tests/test_prune_planner.py` (current coverage is around lines 196-206 and 347-476 only). Add those cases so the documented validation branches are protected."
          ],
          "session": 1,
          "timestamp": "2026-04-17T10:52:04.282Z"
        }
      ],
      "stuckSince": null
    }
  },
  "metadata": {
    "createdAt": "2026-04-17T03:55:31.492Z",
    "lastUpdated": "2026-04-17T10:52:04.282Z"
  }
}